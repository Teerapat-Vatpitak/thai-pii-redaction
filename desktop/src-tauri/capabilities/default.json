{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Deliberately minimal (DESK-3): the global shortcut, clipboard and sidecar work is all Rust-side, which never consults webview capabilities — granting shell/clipboard/global-shortcut here only widens what a webview XSS can reach. tests/test_desktop_capabilities.py pins this.",
  "windows": ["main"],
  "permissions": ["core:default", "core:tray:default", "updater:default"]
}
