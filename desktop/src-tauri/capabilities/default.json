{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Native actions are compiled, typed application commands; the one core grant receives authority invalidation events.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen"]
}
