# Pathumma, TNER, and HTTP live acceptance

- Date: 2026-07-24
- Product version: `2.5.0`
- Source commit before current readiness edits:
  `8e06f9bf48cb745bf83624e7c45a7858d4004416`
- Working tree: dirty; evidence is provisional until repeated from the clean
  handoff commit
- Result: 11 pass, 0 fail, 0 blocked
- Credential values, request text, mappings, and provider bodies recorded: no

Run:

```powershell
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe scripts\run_acceptance.py --live-pathumma --live-tner
```

The backend was started temporarily with `AIGUARD_DEMO=1`, bound only to
`127.0.0.1`, and shut down after the run. Port 8000 had zero listeners after
cleanup.

## Result summary

| Check | Result | Observation |
|---|---|---|
| API health | Pass | Version `2.5.0`, API contract `1`. |
| Demo-enabled path | Pass | HTTP 200 only in the explicitly enabled process. |
| Fake protected roundtrip | Pass | Two entity types; exact restoration; no warning. |
| Prompt-injection guard | Pass | Override and exfiltration signals; warn-only behavior preserved. |
| PDPA report | Pass | One PDF digest recorded; aggregate grade only. |
| PDF redaction | Pass | 13 entities, text-layer route, flattened output. |
| Invalid PDF | Pass | HTTP 400. |
| Pathumma completion | Pass | Non-empty response. Marker/exact echo remained false quality telemetry, not a privacy failure. |
| Pathumma protected roundtrip | Pass | Two entities; provider saw protected text; one returned token and one unused-token warning. |
| TNER live tagging | Pass | 26 tokens, 12 entity tags, live `PER/LOC/ORG/DTM` labels present. |
| TNER pipeline mapping | Pass | Four mapped entity types: `NAME`, `LOCATION`, `ORGANIZATION`, and `DATE`. |

The machine-readable PII-free artifact is gitignored under
`artifacts/acceptance/`. Repeat this command from the final clean candidate so
its `git_dirty` field becomes false before treating it as release-freeze
evidence.
