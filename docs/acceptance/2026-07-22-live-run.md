# Live acceptance record — 2026-07-22

Candidate: product `2.4.0`; the final Desktop acceptance executable was built
from exact Git commit `58bb6ab019739a5e96fed786d237ffaa072d460f`, and this
ledger is maintained on `main`. Environments: Windows, Python 3.13, localhost
FastAPI, Chrome, an isolated Windows desktop build, and live AI for Thai
endpoints. Only repository synthetic fixtures were used; no credentials, source
text, mappings, or provider bodies are recorded.

This is an acceptance record for the current implementation, not permission to
publish another `2.4.0` build. That version is already released; a publishable
candidate must receive a new version after the remaining storefront gates pass.

## Automated API and live-provider acceptance

Command:

```powershell
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe scripts\run_acceptance.py --live-pathumma --live-tner
```

Result: **11 passed, 0 failed, 0 blocked**. PII-free evidence:
`artifacts/acceptance/acceptance-20260722T140353Z.json` (gitignored).
That run predates evidence schema 2: its short commit identifies the baseline
but it does not encode the dirty working tree. It is therefore explicitly
provisional; subsequent records carry the full commit and `git_dirty` state.
A current schema-2 core rerun also passed **7/7** at
`artifacts/acceptance/acceptance-20260722T143812Z.json`; it records the full
baseline SHA and `git_dirty: true`, and proves warn-only integration through a
successful guarded fake-provider roundtrip rather than a constant claim.

| Check | Result | Safe evidence |
|---|---|---|
| API health | Pass | Product `2.4.0`, API contract `1` |
| Playground enabled and hosted wording | Pass | HTTP 200; invalid device-local claim absent |
| Fake protected roundtrip | Pass | 2 entities; exact restore; no warnings |
| Injection guard | Pass | Override and exfiltration warnings returned; request remained warn-only |
| PDPA PDF report | Pass | Valid PDF, grade B |
| Text-layer PDF redaction | Pass | 12 entities; valid previews; flattened output has no extractable text |
| Non-PDF rejection | Pass | HTTP 400 |
| Pathumma completion | Pass | Non-empty decoded completion |
| Pathumma protected roundtrip | Pass | 2 entities; raw synthetic PII absent from provider-visible text; both returned tokens restored |
| TNER live tagging | Pass | Live compact BIO labels returned |
| TNER pipeline mapping | Pass | `NAME`, `LOCATION`, `ORGANIZATION`, and `DATE` produced end to end |

Pathumma marker echo is not deterministic: another accepted run omitted one
marker and returned the documented unused-token warning. That is not a safety
failure because a generative answer may legitimately omit an entity. Acceptance
gates on a decoded completion, no raw PII in provider-visible text, and
restoration of every token that the provider actually returns.

## Full optional environment

A separate clean Python `3.13.12` environment installed core, web, ML, and OCR
extras together. It had one OpenCV owner (`opencv-contrib-python 4.10.0.84`) and
passed `uv pip check`. On the pre-Desktop-follow-up snapshot, the full suite
passed with no skipped or expected-failure tests: **787 passed, 0 skipped,
0 xfailed**. Focused OCR/PDF and semantic/NER suites also passed, and a real Thai
PaddleOCR inference returned non-empty Thai text with mean confidence `0.9847`.
The current tree adds the colon-address detector and rendered-pixel regressions:
its base environment passes **782 tests with 6 optional skips**, and the focused
detection/PDF subset passes **71/71**. The same Python 3.13 full-optional
environment was then rerun against exact commit `58bb6ab`: **789 passed,
0 skipped, 0 xfailed, 0 failed**, and `uv pip check` found all 108 packages
compatible.

Windows load order matters when Torch and PaddleOCR coexist: Torch must load
first. The production availability path already primes Torch, and a regression
test now pins this behavior. The reverse order was reproduced as a Windows DLL
load failure and is not treated as a supported entry path.

## Storefront acceptance

### Extension — Blocked on candidate loading

The versioned package was built from the current source:

- file: `dist/aiguard-extension-2.4.0.zip`
- size: `295064` bytes
- SHA-256: `72050a3a8dcae4e13a274a96b219a1118aa181fd483f47f01c1890d5ed6b1db7`
- contents: 32 entries; manifest present; development README excluded
- automated extension suite: 43 passing tests

Chrome inspection found no AI Guard injection because the candidate extension
was not loaded. Browser automation cannot change `chrome://extensions`, so live
site mask/restore remains **Blocked**, not Pass. The owner must load this exact
candidate and run the declared-site checklist. The ZIP is acceptance evidence
only and must not be distributed as a second `2.4.0` release.

### Desktop — fresh candidate verified locally

An isolated snapshot at `257e72c` produced a Windows NSIS installer and passed
sidecar health/sanitize/process cleanup, release-shell launch, extracted-installer
payload launch, and 19 Rust lifecycle tests.

- installer: `AI Guard_2.4.0_x64-setup.exe`
- size: `78175319` bytes
- installer SHA-256: `62b0ca061132a6ca9a52bfefe1acf0694ec59cebdc7489885a2df835e42ccf7c`
- desktop shell SHA-256: `ac8fa295a79397ac2db601d67fd377464240120a14842cfe333a7a42224841aa`
- sidecar SHA-256: `36b5fd283fc178870d1104efa25114f3ab8241d1990b4378b64fb257aaa44704`

The first build was a rehearsal, not a publishable release candidate: it
predated the fixes completed in exact candidate `58bb6ab`, used local Node 24
while CI pins Node 22, and stopped after producing the installer because the
CI-only Tauri updater signing key was absent. Authenticode remains intentionally
unsigned per project policy; that is separate from Tauri updater signing.

The owner later authorized controlled UI acceptance of both the initial
rehearsal and the fresh exact candidate. Each `desktop.exe` opened one window
titled `AI Guard`, and its packaged `aiguard.exe` sidecar reported health `ok`
at product `2.4.0`. The following synthetic-only checks record the completed
packaged-UI acceptance:

| Desktop check | Result | Evidence / remaining gate |
|---|---|---|
| Token mask and exact restore | Pass | Two entities masked and the original name/phone restored exactly |
| Surrogate mask and exact restore | Pass | Realistic surrogate name/phone restored to the same originals |
| PDPA analysis screen | Pass | Score, grade, Section-26 flag, breakdown, and recommendations rendered |
| PDPA PDF download | Pass | Fresh candidate called `/api/analyze-report`, reported successful download, and produced a valid PII-free PDF with no raw synthetic name or phone |
| PDF upload, preview, and download | Pass | The file chooser accepted the repository fixture; before/after previews rendered; chips showed whitelisted PII type labels instead of `[object Object]`; the localized download completed |
| Redacted PDF opens and has no text layer | Pass | Downloaded one-page PDF opened successfully; extraction returned zero characters and no fixture phone/name |
| Redaction coverage at 300 DPI | Pass | The fresh-candidate output covered the colon-delimited house number `99` as well as the remaining detected values |
| Settings and theme | Pass | Dark theme applied and the fresh candidate showed the corrected ChatGPT/Codex/Gemini/Grok/Perplexity/GLM·Z.ai support copy |
| Audit log | Pass | Refresh populated only time, step, entity count, result, and latency; no raw PII was displayed |
| Updater check | Pass | Completed and reported that the rehearsal was current |
| Global hotkeys | Pass | With owner-approved synthetic clipboard content, `Ctrl+Shift+M` removed the raw name/phone and `Ctrl+Shift+R` restored the exact originals |
| Close/reopen and sidecar release | Pass | Quit closed the app and released port 8000; reopening the same exact candidate produced one AI Guard window and a healthy product-`2.4.0` sidecar |

The first rehearsal exposed three packaged-UI defects: PDF field chips rendered
`[object Object]`, the PDPA screen had no report-download control, and the
colon-delimited house number `99` remained visible. Exact candidate
`58bb6ab019739a5e96fed786d237ffaa072d460f` closes all three in the packaged UI,
localizes the PDF button, and shows the corrected supported-site copy.

That exact isolated build passed version/release-readiness checks,
frozen-sidecar staging, optimized Tauri build, NSIS packing, payload extraction,
and **19/19** Rust tests. After owner approval, the old rehearsal quit and
released port 8000; the fresh executable then launched as one `AI Guard` window
with a healthy packaged sidecar.

- installer: `AI Guard_2.4.0_x64-setup.exe`, 78,197,037 bytes,
  SHA-256 `6ce006699462e89510decaf7c3659da4d3a4861e9dbea7305fa0a376810b5cbe`
- desktop shell: 17,598,976 bytes,
  SHA-256 `8feb1dccd15bc9b358903d4e69f6103787c5a362302c588ba7d68193cf7df2ea`
- sidecar: 74,304,934 bytes,
  SHA-256 `49c7bf4d3ab9a3ba3546276be4c512b220b441960b13a03e02403af25e881248`

This remains a non-publishable `2.4.0` rehearsal. The local build used Node
24.15.0 because Node 22 was unavailable, and it stopped only after the installer
was produced when Tauri requested the CI-only updater signing key. Release CI
must build with its pinned Node 22 and produce the signed updater artifacts.

### Playground — Functional pass; two browser artifact flows pending

Passed in Chrome with synthetic fixtures:

- live detection highlighted four entities in the sick-leave sample;
- fake token and surrogate roundtrips restored exactly;
- the injection fixture warned for instruction override and exfiltration without
  blocking;
- the PDPA report endpoint returned a valid PDF and the UI reported generation;
- layouts at `800x900` and `1366x768` had no horizontal overflow;
- backend-offline handling now clears stale provider state and displays
  `ยังไม่มีคำตอบให้กู้คืน` instead of leaving a false pending result.

The browser download event for the report did not complete within the harness,
and PDF file upload was denied by the Chrome extension's file-URL permission.
Downloading/opening the report and uploading/downloading the redacted PDF remain
manual gates after that permission is enabled.

### PDF — API/download and fresh-candidate visual pass

The text-layer API path, flattening/no-extractable-text condition, previews,
Desktop download/open, and non-PDF 400 response pass. Fresh-candidate output
from exact commit `58bb6ab` was rendered at 300 DPI and confirmed that the
colon-delimited house number `99` is covered. The full optional environment also
passes scanned OCR tests and real Thai PaddleOCR inference. Still pending are
the Playground browser download/open flow, the UI oversized-file path, and
inspection that temporary files disappear after both success and failure.

## Remaining owner-assisted gates

1. Load the exact extension candidate unpacked, enable file-URL access for the
   ChatGPT Chrome Extension, and run live-site/PDF browser checklists.
2. After the remaining storefront gates pass, assign a new release version and
   let CI build/sign the release and updater artifacts from the accepted commit.
   Never publish this duplicate `2.4.0` rehearsal.
