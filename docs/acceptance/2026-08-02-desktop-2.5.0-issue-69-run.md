# Desktop 2.5.0 installed-artifact acceptance — Issue #69

- Evidence finalized (UTC): `2026-08-02T11:07:18Z`
- Evidence level: installed Windows release artifact
- Test checkout commit: `24ca652734a9fb649f18239e53e5c40015fb0d30`
- Release tag target commit: `24914ab54fc8e8338ca529dd5c20a79f51d749c0`
- Release artifact: `AI.Guard_2.5.0_x64-setup.exe`
- Release artifact SHA-256: `2d4780f2ebc2e4555ebe99bbc56aca340543411cc9430d960395095e7684eb7e`
- Application version: `2.5.0`
- Synthetic fixtures: `examples/prompts/01_sick_leave_email.txt` and
  `examples/sample_document.pdf`

The published `v2.5.0` release was present on GitHub and its asset digest
matched the locally downloaded asset. Windows installed-product metadata
reported `2.5.0`; the installed `AI Guard` application launched with one
window and its packaged sidecar served the local API. No installer was rebuilt
or republished.

Raw fixture text, selected text, token mappings, clipboard bodies, provider
responses, credentials, runtime logs, screenshots, generated PDFs, and local
absolute paths are deliberately absent from this record.

## Results

| Test case | Result | PII-free evidence |
|---|---|---|
| `DESK-69-01` exact release, installation, and first launch | PASS | Published artifact name and digest matched; installed product metadata reported `2.5.0`; one `AI Guard` window launched. |
| `DESK-69-02` displayed/backend version and health | PASS | Installed display metadata and `/api/health` agreed on `2.5.0`; health returned HTTP 200 and `ok`. |
| `DESK-69-03` Token Mask/Restore | PASS | Installed UI produced a token-shaped masked result; Restore completed without an error or leftover-token warning. |
| `DESK-69-04` Surrogate Mask/Restore | PASS | Installed UI produced a surrogate-mode masked result; Restore completed without an error or leftover-token warning. |
| `DESK-69-05` PDPA analysis and report generation | PASS | Analysis rendered; the fixed-name report download completed; the PDF was valid, aggregate-only, and contained none of the detected fixture entity values. |
| `DESK-69-06` PDF redaction and flattened output | PASS | Installed UI accepted the repository PDF fixture, rendered before/after output, downloaded a valid PDF, and extraction returned zero characters. |
| `DESK-69-07` global hotkey | PASS | `Ctrl+Shift+M` produced a non-empty masked clipboard result; `Ctrl+Shift+R` restored the synthetic fixture after line-ending normalization. |
| `DESK-69-08` audit output | PASS | Audit view and local endpoint returned summary fields only; no session identifier or source-text field was present. |
| `DESK-69-09` processing receipt companion gate | PASS | The repository's UTF-8 CLI receipt issue/verify path issued and verified a synthetic receipt; Desktop has no receipt API by design, so this is not claimed as a Desktop screen. |
| `DESK-69-10` updater without unintended upgrade | PASS | The installed updater reported that `2.5.0` was current; no install action was invoked. |
| `DESK-69-11` application and sidecar shutdown | PASS | After explicit UI quit, Desktop/sidecar process count was zero and port 8000 had zero listeners. |
| `DESK-69-12` relaunch and final lifecycle cleanup | PASS | Relaunch produced one `AI Guard` window with healthy `2.5.0` backend; final process count and port-8000 listener count were both zero. |

## Defects and scope reconciliation

No product or packaging defect was reproduced in the exact installed artifact;
no runtime code, installer, or release asset was changed. The only
documentation boundary is intentional: processing receipts are a CLI/core
feature with no Desktop API in v1, while the installed Desktop acceptance
covers the audit-log output exposed by the application.

The separate receipt verification was first attempted without UTF-8 mode and
hit a Windows `cp1252` console encoding error. Repeating the same command with
the repository-required `PYTHONUTF8=1` setting passed; this was an environment
invocation issue, not a product failure.

## Lifecycle evidence

- Initial installed launch: one application window; local backend healthy.
- Explicit shutdown: zero Desktop/sidecar processes; zero port-8000 listeners.
- Relaunch: one application window; local backend healthy at `2.5.0`.
- Final shutdown: zero Desktop/sidecar processes; zero port-8000 listeners.

This run accepts the installed Windows `2.5.0` release path for Issue #69.
The Desktop Settings screen does not expose a separate in-app version label;
the displayed-version evidence is the Windows installed-product metadata,
cross-checked against `/api/health`.
