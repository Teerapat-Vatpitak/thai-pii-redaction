# Desktop 2.5.0 installed-artifact acceptance

- Date: 2026-07-24
- Evidence level: installed Windows release artifact
- Result: pass
- Product version: `2.5.0`
- OS: Windows 11 Home Single Language, build `26200`, 64-bit
- Release artifact: `AI.Guard_2.5.0_x64-setup.exe`
- Release SHA-256:
  `2d4780f2ebc2e4555ebe99bbc56aca340543411cc9430d960395095e7684eb7e`
- Installed location: `%LOCALAPPDATA%\AI Guard`

The artifact name and digest were read from the published `v2.5.0` GitHub
release. Windows uninstall metadata reported `DisplayVersion=2.5.0`, and the
running `/api/health` response reported `version=2.5.0`.

Only repository synthetic fixtures were used. Raw fixture values, mappings,
clipboard contents, and provider bodies are deliberately absent from this
record. Visual states were inspected during the operator run; screenshots were
not committed because the acceptance repository is designed to remain
PII-free.

## Results

| Scenario | Result | Evidence |
|---|---|---|
| Clean launch and health | Pass | One Desktop process started the packaged PyInstaller sidecar tree; health returned `ok`, version `2.5.0`. |
| Token Mask/Restore | Pass | Three detected entity types were replaced with tokens and restored exactly. |
| Surrogate Mask/Restore | Pass | Three detected entity types were replaced with synthetic surrogates and restored exactly. |
| Global hotkeys | Pass | `Ctrl+Shift+M` replaced the clipboard content with tokens; `Ctrl+Shift+R` restored it exactly. |
| PDPA analysis/report | Pass | Aggregate analysis rendered and the downloaded one-page report contained no email marker or fixture values. |
| PDF redaction | Pass | The text-layer sample produced 13 fields and complete visual black-box coverage. The downloaded one-page PDF had zero extractable characters. |
| Settings | Pass | Token/surrogate selection and light/dark theme changes worked. |
| Audit view | Pass | Current sanitize, restore, analyze/report, and PDF actions appeared with time, counts, status, and latency only. |
| Updater | Pass | Reported that `2.5.0` was current. |
| Close/reopen | Pass | After explicit quit, Desktop and sidecar process counts and port-8000 listeners were all zero. Reopen restored health and the local UI. |

Downloaded acceptance artifacts were inspected locally:

| Artifact | SHA-256 | Privacy check |
|---|---|---|
| Redacted sample | `A3E9CC401330FE2A71C546BB1E4B8B597BBB000CF4D8F327266010D8995E026D` | Zero extractable characters. |
| PDPA report | `2DD8C9BB8951F0CF707C57494C85350BF721D5C61A90821F35C9754DC6159135` | Aggregate-only report; no email marker. |

The two downloaded files remain outside the repository under the operator's
Downloads directory. They are evidence artifacts, not product fixtures.

## Scope boundary

This run accepts the installed Windows Desktop release path. It does not
replace the separate official AI for Thai deployment gate and does not claim a
Marketplace, macOS, or Linux GUI run.
